.lists
  - if current_user.is_student?
    css:
      .main-descs .main-desc {width: 870px;margin: 0 auto}
  = link_to_home
  - if current_user.is_student?
    .title style='height: auto;' = "#{@homework.short_name}【#{@homework.status_name_for(@student_homework)}】"
    .hresult
    .main-descs
      .flt.fleft
      .frt.fright
      .main-desc
        = render 'student'
  - else
    .title style='height: auto;' = "#{@homework.full_name}"
    .main-descs
      = render 'teacher' 
= render 'level'
