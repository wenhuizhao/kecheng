<h2>忘记密码?</h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :手机号码 %><br />
  <%= f.number_field :phone, autofocus: true %></div>

  <div><%= f.submit "发送重置" %></div>
<% end %>

<%= render "devise/shared/links" %>
